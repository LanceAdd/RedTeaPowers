interface:
  display_name: "Requesting Code Review"
  short_description: "Request focused code review before merging."
  default_prompt: "Use $requesting-code-review to prepare and request focused code review for this implementation."
